[tool.poetry]
name = "osupp"
version = "0.0.1"
description = "osu! PP Calculation for Python"

[tool.poetry.dependencies]
python = "^3.10"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
